import type { Socket } from "socket.io";

export const betHandler = async (socket: Socket, payload: string) => {
    try {

    } catch (error: any) {
        console.error("error occured:", error.message);
    }
}